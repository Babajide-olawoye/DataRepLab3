//imports needed
import React from 'react';

//exporting file so i could use is app.js
export class header extends React.Component {
    //render method enabling me to use html 
    render() {
        return (
            <div>
                <h1>This is the header component</h1>
            </div>

        );
    }
}